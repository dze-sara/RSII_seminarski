<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Rentacar.Mobile.Views.RegisterNowPage">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Label Text="Please register now in order to use application!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Entry x:Name="firstNameEntry" Text="{Binding FirstName}" VerticalOptions="Center" Placeholder="first name"/>

            <Entry x:Name="lastNameEntry" Text="{Binding LastName}" VerticalOptions="Center" Placeholder="last name"/>

            <Entry x:Name="emailEntry" Text="{Binding Email}" VerticalOptions="Center" Placeholder="email"/>

            <Entry x:Name="passwordEntry" Text="{Binding Password}" VerticalOptions="Center" IsPassword="true"/>
            
            <Button VerticalOptions="Center" Text="register now" Command="{Binding RegisterNowCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>